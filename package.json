{
  "name": "tree-sitter-radb",
  "version": "1.0.0",
  "description": "Tree-sitter grammar for radb",
  "scripts": {
    "test": "tree-sitter test",
	  "playground": "tree-sitter generate && tree-sitter build --wasm && tree-sitter playground",
	  "build-grammar": "tree-sitter generate && tree-sitter build",
	  "install": "tree-sitter generate && tree-sitter build && tree-sitter build --wasm"
  },
  "keywords": [
    "parsing"
  ],
  "author": "Boris Glavic",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "tree-sitter-cli": "^0.26.5"
  }
}
